export default function Titulo({ children }) {
    return (
        <h1 className="font-semibold justify-center text-gray-900 dark:text-gray-100 text-2xl">
        {children}
        </h1>
    )

}